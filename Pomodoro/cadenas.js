/*let cadena='Cadena de prueba';
let cadena2 = ' mas cadenas';
document.write('<h1>'+cadena.length+'</h1>');*/
/*
      concat(): Concatena dos o mas cadenas
      startsWith(): Si una cadena comienza con los caracteres de otra cadena devulve true si no false.
      endsWith():  Si una cadena termina con los caracteres de otra cadena devulve true si no false.
      includes(): Si la cadena puede encontrarse dentro de otra cadena devulve true si no false}.
      indexOf(): devuleve el indice del primer caracter de la cadena, si no exite devulve -1.
      lastIndexOf(): devuleve el ultimo indice del primer caracter de la cadena, sin no exite devulve -1.

      padStart(): [propuesta estandar] rellenar cadena al principio con los carcateres deseados.
      padEnd(): [propuesta hecha] rellenar cadena al final con los carcateres deseados. 
      repeat(): devulve la misma cadena pero repetida la cantidad que le indiquemos. 
      
      split(): divide la cadena como le pidamos.
      subsString(): nos reotorna un pedazo de la cadena que seleccionamos.
      toLowerCase(): convierte una cadena a minuscula.
      toUpperCase(): convierte una cadena a mayuscula.
      toString(): metodo devuelve una cadena que representa al objeto especificado.
      trim(): elimina los espacios al principio y al final de la cadena.
      trimEnd(): elimina los espacios al final de la cadena.
      trimStart(): elimina los espacios al principio de la cadena.
      valueOf(): retorna el valor primitivo de un objeto String.



      ===metodos para arreglos===
      _______Transformadores____      

      pop(): elimina el ultimo elemento del array
      shift(): elimina el primer elemento del array del array
      push(): agrega un elemento al array al final. 
      reverse(): invierte el orden de los elementos de un array
      unshift(): agrega uno o mas elementos al inicio del array y devulve la nueva longitud del array devulve la nueva longitud del array del array
      sort(): ordena los elementos de un array localmente y devulve la nueva longitud del array
      splice(): cambia el contenido de un array eliminando elementos existentes y/o agregando nuevos elementos 

      _______Accesores________

      join(): une todos los elementos de una matriz en una cadena y lo devulve.
      slice(): devuelve una parte del array dentro de un nuevo array empexando por inicio hasta fin (fin no incluido).      

      _______De repetición________

      filter(): crea un nuevo array con todos los elementos que cumplan la condicion.
      forEach(): ejecuta la funcion indicada una vez por cada elemento del array
*/